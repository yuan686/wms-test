# 选用AI编程工具

Cursor （写需求）+ Trae（改bug）

## 使用方式

新需求，编写需求说明，提供AI，描述预期以及边界问题

改bug，指明有问题的文件或代码行，提出存在bug的问题，让其进行修改（必要时提供修改方案）

优化需求，提供需要优化的功能以及优化方案

# AI编写代码遇到的问题

1、生成的代码携带不相关的上下文信息，解决方案新建窗口

2、AI新增和优化的代码存在问题，撤销后，重新修改提示词步骤再细分，再让AI自检

3、免费版的AI生成代码效率太慢，在生成代码的同时，可以先整理需求说明书以及AI需要自检的问题，以及针对本项目下需要遵守的团队代码规范编写

# 有时间优化将优化的地方

出库申请从已有的库存中进行选择，减少不存在的库存商品做出库申请

编写出库查询页面

删除商品时，根据布隆过滤器+分布式锁，再判断数据库，提高并发环境下的性能

根据业务需要判断是否需要引入MQ，判断出库操作下游是不是有多步操作，可以在预扣减库存后，发送MQ去处理执行

# 任务3：bug排查

根据问题进行复现，找到存在问题的文件或接口

前端根据路由定位到所在的文件，然后找到对应函数，修改数据后传递当前页码，避免跳转到其他页

后端根据接口定位Controller，从而定位Service，判断库存表是否存在数据

# 选做任务A：出库单并发扣减方案

## 实现内容

- 新增 `POST /api/outbound-orders` 创建出库单接口。
- 新增出库单主表、明细表对应的实体、DTO 和 Repository。
- 新增前端“出库管理”页面，支持客户名称、多行出库明细、商品搜索、仓库和库位联动选择。
- 出库创建成功后，出库单状态为 `COMPLETED`，数据库库存同步扣减。

## 并发控制方案

本次采用“Redis Lua 原子预扣减 + 数据库条件扣减 + 失败补偿”的方案防止超卖。

1. Redis 库存 key 使用 `inventory:stock:{productId}:{locationCode}`。
2. 创建出库单时，先把同一商品和同一库位的重复明细合并，避免同一请求内重复判断库存。
3. 使用 Lua 脚本在 Redis 内原子完成库存读取、库存充足判断和 `DECRBY` 扣减。
4. Redis key 不存在时，从数据库读取当前库存，并使用 `SETNX` 初始化，避免并发初始化时覆盖其他请求已扣减后的库存。
5. Redis 预扣成功后，在数据库事务内创建出库单、保存明细，并执行条件更新扣减库存。
6. 如果数据库保存或扣减失败，对已经 Redis 预扣成功的库存执行 `INCRBY` 补偿回滚。

## Lua 返回码

- `-2`：Redis 库存 key 不存在，需要从数据库初始化后重试。
- `-1`：Redis 当前库存不足，直接拒绝出库。
- `>= 0`：扣减成功，返回扣减后的 Redis 剩余库存。

## 防止超卖的理由

Redis Lua 脚本在 Redis 单线程模型内执行，同一个库存 key 的检查和扣减不会被其他请求插入，因此高并发下不会出现多个请求同时读到同一份库存并重复扣减的问题。

数据库侧仍保留最终保护：

```sql
UPDATE inventory
SET quantity = quantity - ?
WHERE product_id = ?
  AND location_code = ?
  AND quantity >= ?
```

这条条件更新保证数据库库存不足时不会扣成负数。即使 Redis 缓存缺失、Redis 重启后重新初始化，或数据库库存被外部流程修改，数据库条件扣减也能作为最后一道防线。

## 失败处理

- 商品不存在、库位不存在、数量非法、库存不足时抛出业务异常。
- Redis 预扣失败时不会写入出库单。
- Redis 预扣成功但数据库事务失败时，会按已扣减明细回滚 Redis 库存，避免 Redis 可用库存被永久少算。
- 数据库条件扣减失败时，出库单事务回滚，同时 Redis 预扣库存补偿回滚。

## 缓存穿透 / 击穿 / 雪崩防护

出库申请在 `OutboundApplyCacheService` 中统一处理缓存风险：


| 问题   | 场景                     | 方案                                                                                                       |
| ---- | ---------------------- | -------------------------------------------------------------------------------------------------------- |
| 缓存穿透 | 恶意或重复查询不存在的商品 ID、库位编码  | 对不存在数据写入短 TTL 空值标记（`wms:outbound:null:product:{id}`、`wms:outbound:null:location:{code}`），后续请求直接拦截，不再打数据库 |
| 缓存击穿 | 热点库存 key 过期瞬间大量并发回源    | 使用 Redis 分布式锁（`inventory:stock:{productId}:{locationCode}:init-lock`），仅一个线程从数据库加载并回填，其余线程轮询等待缓存就绪        |
| 缓存雪崩 | 大量库存 key 同时过期导致数据库压力激增 | 库存缓存 TTL = 基础时间 + 随机抖动（默认 1800s + 0~600s），打散过期时刻                                                         |


补充说明：

- 库存缓存 key 仍为 `inventory:stock:{productId}:{locationCode}`，加载时使用 `SETNX` + 随机过期时间。
- 入库累加库存后会主动 `evict` 对应库存缓存，避免长期脏读。
- 相关参数可在 `application.yml` 的 `wms.outbound.cache` 下调整。

